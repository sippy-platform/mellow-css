import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";

import { Checkbox } from "./Checkbox";

<Meta
  title="Forms/Checkbox"
  component={Checkbox}
/>

export const Template = (args) => <Checkbox {...args} />;

# Checkbox
The Checkbox component provides an easy to use checkbox.

<Canvas>
  <Story
    name="Basic checkbox"
    args={{
      label: 'Basic checkbox',
      id: 'basic',
      checked: true
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Helper checkbox
Add a helper property to add a hleper text underneath.

<Canvas>
  <Story
    name="Helper checkbox"
    args={{
      label: 'Helper checkbox',
      id: 'helper',
      helper: 'This extra info helps you pick the right thing',
      checked: true
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

# Indeterminated
Make the checkbox indeterminate by setting `indeterminate` as the value of the `checked` prop.
<Canvas>
  <Story
    name="Indeterminated checkbox"
    args={{
      label: 'Indeterminated checkbox',
      id: 'indeterminate',
      checked: 'indeterminate'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

# No label
Leave out the label to get just a checkbox.

<Canvas>
  <Story
    name="No label"
    args={{
      id: 'none',
      checked: true
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
