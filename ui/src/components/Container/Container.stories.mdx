import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";

import { Container } from "./Container";

<Meta
  title="Layout/Container"
  component={Container}
/>

export const Template = (args) => <Container {...args} />;

# Container
Use the `Container` component to position your layout.

<Canvas>
  <Story
    name="Container"
    args={{
      children: (
        <div className="bg-accent p-2 rounded text-light">This is the container's child component.</div>
      )
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

# Fluid container
Use the `fluid` prop to make the container fluid.

<Canvas>
  <Story
    name="Fluid container"
    args={{
      fluid: true,
      children: (
        <div className="bg-accent p-2 rounded text-light">This container is fluid.</div>
      )
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

# Responsive container
Use the `fluid` prop to make the container fluid at any given breakpoint.

<Canvas>
  <Story
    name="Responsive container"
    args={{
      fluid: 'md',
      children: (
        <div className="bg-accent p-2 rounded text-light">This container is fluid until the medium breakpoint.</div>
      )
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

# API
<ArgsTable of={Container} />
