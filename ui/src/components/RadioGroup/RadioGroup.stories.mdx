import { useState } from 'react';
import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { Radio, RadioGroup } from '../';

<Meta
  title="Forms/RadioGroup"
  component={RadioGroup}
/>

export const Template = (args) => <RadioGroup {...args} />;

# Radio group
When using the Radio component, it must be used within a RadioGroup component. The RadioGroup component controls the state for the other components.

<Canvas>
  <Story
    name="Basic radio"
  >
    {() => {
      const [value, setValue] = useState("hello");
      return (
        <RadioGroup
          onChange={setValue}
          value={value}
          label="Value checker"
        >
          <Radio value="hello" label="Hello" />
          <Radio value="bonjour" label="Bonjour" />
        </RadioGroup>
      );
    }}
  </Story>
</Canvas>

## With helper
Add a helper prop to add extra information.

<Canvas>
  <Story
    name="Helper radio"
  >
    {() => {
      const [value, setValue] = useState("hello");
      return (
        <RadioGroup
          onChange={setValue}
          value={value}
          label="Helper options"
        >
          <Radio value="hello" label="Hello" helper="Hello" />
          <Radio value="bonjour" label="Bonjour" helper="Bonjour" />
        </RadioGroup>
      );
    }}
  </Story>
</Canvas>

## No label
If, for any reason, you ever find yourself in a situation where a radio button doesn't need a label, you can leave it out.

<Canvas>
  <Story
    name="No label radio"
  >
    {() => {
      const [value, setValue] = useState("hello");
      return (
        <RadioGroup
          onChange={setValue}
          value={value}
        >
          <Radio value="hello" />
          <Radio value="bonjour" />
        </RadioGroup>
      );
    }}
  </Story>
</Canvas>
