import { useState } from 'react';
import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { Range } from './Range';

<Meta
  title="Forms/Range"
  component={Range}
/>

export const Template = (args) => <Range {...args} />;

# Range
The slider is a great input type for when your values lie on a scale.

## Orientations
### Basic slider
<Canvas>
  <Story
    name="Basic slider"
  >
    {() => {
      const [value, setValue] = useState(50);
      return (
        <Range
          value={value}
          onChange={(e) => setValue(e.target.value)}
        />
      );
    }}
  </Story>
</Canvas>

## Values
A default slider component will provide a range between 0 and 100 with steps of 1. This cal all be changed. The following example has a minimum value of 10, a maximum value of 20, and takes steps of 2.

<Canvas>
  <Story
    name="Custom values"
  >
    {() => {
      const [value, setValue] = useState(50);
      return (
        <Range
          value={value}
          onChange={(e) => setValue(e.target.value)}
          min={10}
          max={90}
          steps={10}
        />
      );
    }}
  </Story>
</Canvas>

# API
<ArgsTable of={Range} />
